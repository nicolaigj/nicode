{
    "name": "nicode",
    "scripts": {
        "build": "mkdir -p dist && elm make src/Main.elm --optimize --output=dist/main.js && cp index.html dist && cp -r assets dist"
    },
    "dependencies": {
        "elm": "^0.19.1-3"
    }
}
