{
    "name": "nicode",
    "scripts": {
        "build": "mkdir -p dist && elm make src/Main.elm --optimize --output=dist/main.js && cp index.html dist"
    },
    "dependencies": {
        "elm": "^0.19.1-3"
    }
}
