## sveltekit or elm to heroku

### Push a subdir

```bash
git subtree push --prefix <subfolder> origin <branch>

# for svelte directory on heroku remote master
git subtree push --prefix svelte heroku master
```
